#include <iostream>
#include <set>
#include<string>
using namespace std;
int main(){
    set<string> s;
    string abc;
    cin>>abc;
    for(int i=0; i<abc.length(); i++){
        for(int j=1; j+i<=abc.length(); j++){
            s.insert(abc.substr(i,j));
        }
    }
    cout<<s.size();
}
